/// The client's version.
///
/// Keep in sync with `pubspec.yaml` (`version:`). It is intentionally a plain
/// constant so no platform plugin is needed just to read it.
const String appVersion = '1.1.3';
